{
  "name": "blended-teams",
  "private": true,
  "description": "Root tooling for validating content records. The web app has its own package.json in web/.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-content.mjs"
  },
  "devDependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1"
  }
}
